# NexusLedger — support page

The page App Store Connect points at as the app's **Support URL**.

One file, `index.html`, with no dependencies. It is served by GitHub Pages from this repository's
`main` branch. Nothing else about the app lives here.

Edit `index.html` and push; Pages redeploys on its own.
